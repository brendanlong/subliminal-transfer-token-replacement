name: ekfac-validate
setup: uv sync --frozen
command: bash ekfac_val.sh
gpus: 1
use_shared: true
priority: 1
max_runtime_min: 120
env:
  PYTHONUNBUFFERED: "1"
secrets: [HF_TOKEN]
outputs:
  - path: ekfac-out
    s3: s3://brendanlong-experiments/subliminal/ekfac-validate/{job_id}
