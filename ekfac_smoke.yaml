name: ekfac-scale
setup: uv sync --frozen
command: bash ekfac_scale.sh
gpus: 1
use_shared: true
priority: 10
max_runtime_min: 120
env:
  PYTHONUNBUFFERED: "1"
secrets: [HF_TOKEN]
outputs:
  - path: ekfac-out
    s3: s3://brendanlong-experiments/subliminal/ekfac-scale/{job_id}
