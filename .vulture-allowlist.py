# Names vulture cannot see the use of. Regenerate with:
#   uv run vulture subliminal_transfer tests --make-whitelist
# Keep this short: if an entry is genuinely unreachable, delete the code instead.

gradient_checkpointing_kwargs  # Protocol method parameter, matched structurally
idx  # stable row id in the published numbers.jsonl / scored.jsonl
_.use_cache  # transformers generation config, set so checkpointing is legal
peak_gpu_gb  # written to teacher_eval.json via **summary.model_dump()
seconds  # likewise
